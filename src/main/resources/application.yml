spring:
  application:
    name: online-examination-management

  datasource:
    url: jdbc:postgresql://localhost:5432/online_examination_db
    username: postgres
    password: qweasdzxc
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

  sql:
    init:
      mode: always

server:
  port: 8080


---

